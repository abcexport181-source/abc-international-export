// Re-export from the shared context so all existing imports continue to work.
// The actual implementation lives in WebsiteDataContext.tsx.
export { useWebsiteData } from '@/context/WebsiteDataContext';
